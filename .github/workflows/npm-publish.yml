name: Publish to npm

on:
  push:
    branches:
      - main  # Set this to the name of your default branch

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16'  # Set this to the Node.js version you need
        registry-url: 'https://registry.npmjs.org/'

    - name: Install Dependencies
      run: npm install

    - name: Build (if needed)
      run: npm run build  # Run this if you have a build step

    - name: Publish to npm
      run: npm publish
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
